## Instructions to run this project locally

1. clone the github repo
2. create an .env file with PORT and DB_URL (mongodb uri)
3. run npm install
4. run npm run dev
5. hit the http://localhost:PORT/api/shareprice?company=IBM